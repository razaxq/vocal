<div align="center">

<img src="build/icon.svg" width="88" alt="Vocal 图标">

# Vocal

**按住快捷键说话，将语音转成文字，输入到你正在使用的应用中。**

适合聊天、写邮件、记录想法和起草文档。语音识别在电脑本地完成。

[下载最新版](https://github.com/razaxq/vocal/releases/latest) · [开始使用](#开始使用) · [常见问题](#常见问题)

</div>

## 下载与安装

目前提供 **Windows 64 位（x64）版本**，需要麦克风。首次使用时需要联网下载识别模型。

**推荐使用安装版**：在 [下载页面](https://github.com/razaxq/vocal/releases/latest) 选择名称包含 `Setup`、后缀为 `.exe` 的文件，下载后按向导安装。

| 版本 | 如何使用 |
|---|---|
| **安装版（推荐）**：`Vocal-Native-Setup-版本号.exe` | 按向导安装，适合日常使用 |
| 解压版：`Vocal-Native-版本号-win-x64.zip` | 解压后运行 `vocal-native.exe`，无需安装 |

下载页中的 `SHA256SUMS.txt` 和 `Source code` 无需手动下载。

v1.0.0 改用 Qt 原生界面。旧版用户请手动安装新版；旧版配置和历史不会自动迁移。

## 开始使用

1. **打开 Vocal。** 进入「识别」设置。之后可双击系统托盘中的 Vocal 图标打开设置。
2. **下载模型。** 在「识别」页下载已选中的定稿、同音纠错和标点模型，等待下载完成。
3. **检查麦克风。** 在同一页选择麦克风，点击「测试」，说话时确认音量有变化。
4. **试着输入。** 打开记事本，点击输入位置，按住键盘右侧的 <kbd>Ctrl</kbd> 说一句话，松开后等待文字输入。

默认情况下，每句话完成识别和纠错后，会显示在悬浮窗并追加到输入位置。
键盘触发录音时可按 <kbd>Esc</kbd> 取消本次输入；为了便于撤回，录音期间请尽量不要手动移动光标或编辑同一段文字。

「触发方式」中可独立开关键盘和鼠标触发，也可同时启用。鼠标支持左键、中键（按下滚轮）或左键＋中键：按住等待后说话，松开结束。延迟默认 1 秒，可自行调整；鼠标录音不使用 Esc 取消。中键可能触发目标应用的滚动功能。

全屏应用中默认不触发录音；可在「触发方式」中分别允许键盘或鼠标在全屏时使用。

## 常用功能与设置

| 想要做什么 | 在哪里设置 |
|---|---|
| 更换快捷键、使用鼠标，或改为按一下开始、再按一下结束 | 设置 → 触发方式 |
| 更换麦克风、语言模型，或调整说话停顿时间 | 设置 → 识别 |
| 提高人名、术语的识别机会 | 设置 → 识别 → 个人热词，每行一个，点击保存 |
| 使用雾凇基础词库 | 设置 → 识别 → 雾凇词库，默认开启 |
| 减少「嗯」「呃」等口头语 | 设置 → 口语清理，可调整力度或关闭 |
| 将口述文字整理为书面表达 | 设置 → AI 整理，需要配置并启用 AI 服务 |
| 开机后自动运行 | 设置 → 通用 → 开机自启 |
| 更换深色、浅色主题 | 设置 → 外观 |
| 查看之前的识别记录 | 设置 → 历史 |

默认关闭流式预览，使用 **Paraformer 三语**识别、**MacBERT**纠正中文同音错字。
如果经常输入英文、粤语或其他语言，可以在「识别」页根据模型的语言说明更换选择。

## 软件更新

「识别」页使用[雾凇拼音基础词库](https://github.com/iDvel/rime-ice)的完整词表，按本句读音查找相关词，辅助定稿。词库每周检查更新，离线可用，不会覆盖个人热词。
雾凇词库用于 Zipformer 定稿和同音纠错；个人热词可辅助 Zipformer 定稿，并在纠错时受保护。填写后点击「保存热词」，下次录音生效。

Vocal 启动时自动检查更新。默认开启自动更新，发现新版后自动下载并打开安装程序，按向导完成更新。

可在「通用」页关闭自动更新，软件仍会检查并提示，随后在「关于」页手动更新。更新日志位于「关于」页底部。

## 隐私与联网

- **语音识别在本机完成，不上传录音。** 模型下载完成后，基础语音输入无需账号，也可以离线使用。
- **历史记录保存在你的电脑上。** 可以在「历史」页查看和删除。
- **AI 整理是可选功能。** 配置并启用后，待整理的文字会发送到你指定的 AI 服务；不配置该服务也能正常使用基础语音输入。
- **同音纠错可在「识别」页开启。** 可选 MacBERT 或轻量 BERT，下载后在本机处理；可能误改，可随时关闭。
- **模型下载、软件更新和词库更新需要联网。** 下载来源包括 GitHub 和 Hugging Face；词库更新不会上传录音、历史或个人热词。

## 常见问题

### 模型下载慢或失败怎么办？

先确认网络可以访问 GitHub；纠错模型需访问 Hugging Face。软件会显示下载进度和失败提示。
部分模型下载包较大，下载大小与安装后的占用可能不同，以界面显示为准。

### 说话后没有文字怎么办？

先确认「识别」页所需模型已下载完成，并通过麦克风测试检查是否收到了声音。
然后在普通权限运行的记事本中试用，确认光标位于输入框内。
部分应用或以管理员权限运行的窗口可能无法接收文字。

### 识别结果不准确怎么办？

尽量减少背景噪声，选择与说话语言匹配的模型。常用的人名、地名和专业术语可以加入热词表。
识别结果仍可能有误，发送消息或保存重要内容前，请检查文字。

### 还没说完就断句了怎么办？

在「识别」页调大「停顿多久算一句」，为思考和换气留出更长时间。

### 软件占用内存较多怎么办？

识别模型运行时需要占用内存。可以在「识别」页设置「空闲释放内存」；不需要实时预览时，也可以关闭流式模型，仅保留定稿模型。
当前资源占用可在「关于」页查看。

### 安装时提示无法验证发布者怎么办？

当前安装包尚未进行代码签名，Windows 可能显示此提示。请确认文件来自本项目的 [下载页面](https://github.com/razaxq/vocal/releases/latest)。

## 反馈与项目

遇到问题或有功能建议，可以在 [Issues](https://github.com/razaxq/vocal/issues) 中反馈，附上软件版本、操作步骤和错误提示。

由 [Ramos](https://blog.dtft.net/about/) 开发。

Vocal 自有代码采用 [MIT 许可](LICENSE)；随附雾凇词库数据采用 [GPL-3.0 许可](resources/dictionaries/rime-ice/LICENSE)，[来源与源码](resources/dictionaries/rime-ice/SOURCE.md) 可查阅。其他第三方组件遵循各自许可。
感谢 [sherpa-onnx](https://github.com/k2-fsa/sherpa-onnx)、[ququ](https://github.com/yan5xu/ququ)
和 [vocotype-cli](https://github.com/233stone/vocotype-cli) 提供的模型、工具与参考思路。

希望参与开发，可阅读 [开发指南](docs/DEVELOPMENT.md)。
